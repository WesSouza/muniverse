#!/usr/bin/env node

const reason = `
We are still waiting on instructions
from God on how to securely connect to
muniverse.

The earliest protocol still open has
not yet been invented, but a workaround
might be possible soon.

We were ordered to reserve this package
name until then.
`;

console.error(reason);

process.exit(1);
